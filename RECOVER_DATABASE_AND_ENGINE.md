# URGENT: database.py and parser_training_engine.py were truncated to 0 bytes

Disk was full during the P0 learn-globally fix. Restore immediately:

```powershell
cd "C:\Users\caspe\OneDrive\Documenten\MRVAULT AI"
git checkout HEAD -- database.py parser_training_engine.py
```

Then apply the Sprint 50.3 reference re-evaluate fix (see agent chat).
